#!/bin/sh

set -e

# Path relative to $HOME (aka where to cd inside vagrant).
pwd=`realpath $PWD | sed "s@$HOME/@@"`

# .. cd here so vagrant knows what vagrant is the only vagrant <_<
cd ~/work/unixy/vagrant

# Make it so!
exec vagrant ssh -tt -- "exec sh -c 'cd $pwd && $@'"
