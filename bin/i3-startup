#!/bin/sh

set -e

echo "I am $0 . . ."

my-gnome-startup

# Yes, plz.
xautolock \
    -time 10 \
    -locker ~/bin/i3-lock \
    -detect-sleep \
    -notify \
    -notifier "'notify-send -u critical -t 10000 -- 'LOCKING screen in 30 seconds'" &

# Compose key FTW
setxkbmap -option compose:caps
