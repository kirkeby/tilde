#!/bin/sh

while true
do
    # Desktop background
    f="`find ~/ownCloud/desktop-backgrounds/ -type f | shuf -n 1`"
    echo "randomly chosen background: $f"
    feh --bg-max "$f"

    # Naptime!
    sleep 900
done
